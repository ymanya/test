general:
  artifacts:
    - "platforms/android/ant-build/CordovaApp-debug.apk"
checkout:
  pre:
    - echo "pre"
  post:
    - echo "post"
machine:
  java:
    version: openjdk7
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
    test: $CIRCLE_SHA1
dependencies:
  override:
    - echo "nothing to do in depend"
test:
  override:
    - echo "nothing to do in test."
deployment:
  master:
    branch: master
    commands:
      - echo $test
  staging:
    branch: /.*/
    commands:
      - cd www
      - ls